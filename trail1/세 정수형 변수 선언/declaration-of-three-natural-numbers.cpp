#include <iostream>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    int a = 7;
    int b = 23;
    int c = 30;

    std::cout << a << " + " << b << " = " << c;

    return 0;
}