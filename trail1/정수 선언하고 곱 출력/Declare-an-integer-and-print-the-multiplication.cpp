#include <iostream>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    int a = 26, b = 5;
    std::cout << a << " * " << b << " = " << a * b;

    return 0;
}